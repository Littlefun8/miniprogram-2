@startuml
start
:校友填写并提交职位信息;
:系统校验职位字段完整性;
if (字段完整?) then (是)
  :调用 postJob;
  :写入待审核职位;
  :教师查看待审核列表;
  :调用 auditJob;
  if (审核通过?) then (是)
    :更新职位状态为已发布;
  else (否)
    :更新职位状态为已拒绝;
  endif
  :学生浏览职位列表;
  :打开职位详情;
  :系统按业务状态展示职位信息;
else (否)
  :提示补全职位信息;
endif
stop
@enduml

