# 交接报告 — 2026-04-13 开发会话

> 两个分支，分阶段完成

## 分支 1：`feature/seed-data-and-improvements`

> 基于：`main` (commit `e21b5da`)
> 提交：`77ea081`, `01622ef`, `69a7a43`

### 完成内容

1. **丰富种子数据** — 12 个岗位添加 recommenderComment + 扫地机器人岗位
2. **修复 initJobs** — where({}).remove() 兼容 + 云环境 ID 笔误
3. **清理** — 删除 quickstartFunctions、nul 文件
4. **实施文档** — `docs/implementation-job-detail-redesign.md`（职位详情页改版设计）

## 分支 2：`feature/job-detail-redesign`（当前分支）

> 基于：`feature/seed-data-and-improvements`
> 提交：`8f28693`, `79a877a`

### 完成内容

#### 任务 1：职位详情页改版 ✅

**删除的模块**：外部二维码、"展开关联"按钮、截图水印、关联信息

**新增的模块**：
- 岗位描述区（职责 + 要求）
- 内推通道区（状态驱动，4 种状态）：
  - 未申请：锁定状态，显示灰色内推码/链接/微信 + 🔒
  - 待审核：黄色提示"申请审核中"
  - 已通过：绿色解锁，显示内推码+链接+微信（各带复制按钮）
  - 已拒绝：红色提示
- 底部按钮根据状态动态切换（申请内推/审核中/分享给同学/查看其他职位）
- 分享功能：微信转发 + 生成职位卡片（当前为文字复制降级方案）

**涉及文件**：
- `miniprogram/pages/job_detail/job_detail.wxml` — 完全重写
- `miniprogram/pages/job_detail/job_detail.js` — 重写，新增状态驱动逻辑
- `miniprogram/pages/job_detail/job_detail.wxss` — 重写样式

#### 任务 2：职位列表筛选优化 ✅

- 选中城市后按钮显示城市名（如"北京"），选中"全部"恢复"城市"
- 选中岗位后按钮显示岗位名（如"前端开发"），选中"全部"恢复"岗位"
- 激活状态按钮变为主题蓝色

**涉及文件**：`job_list.wxml`, `job_list.wxss`

#### 任务 3：申请门槛 ✅

**硬门槛（资料完善度）**：
- 申请前调用 getUserProfile 检查 nickName + department + major
- 不完善则弹窗引导去编辑资料页
- 无法跳过

**软门槛（专业/年级匹配）**：
- 校友发布职位时可设置"期望专业"和"最低年级"（可选）
- 学生申请时对比自己的专业/年级
- 不匹配弹窗警告"通过率可能较低"，但仍可继续申请

**涉及文件**：
- `miniprogram/pages/job_detail/job_detail.js` — 申请流程拆分
- `miniprogram/pages/post_job/post_job.js` + `.wxml` — 新增期望条件表单
- `cloudfunctions/postJob/index.js` — 存储新字段

#### 任务 4：云函数适配 ✅

- `getJobDetail`：敏感字段过滤增加 jobLink
- `toggleFavorite`：支持 checkOnly 查询模式（不切换，只返回状态）

---

## 当前项目状态

### 可正常运行的功能

| 功能 | 状态 | 说明 |
|------|------|------|
| 用户登录/注册 | 正常 | login 云函数，自动注册 |
| 角色选择 | 正常 | setUserRole 云函数，一次性选择 |
| 职位列表 | 正常 | getJobList，分页+搜索+筛选 |
| 职位详情 | 正常 | getJobDetail，含内推者有话说 |
| 发布职位 | 正常 | postJob，含 recommenderComment |
| 申请职位 | 正常 | applyJob，含简历快照 |
| 申请进度 | 正常 | getApplications，分角色查看 |
| 申请处理 | 正常 | updateApplicationStatus |
| 教师审核 | 正常 | auditJob |
| 通知列表 | 正常 | getNotifications |
| 收藏功能 | 正常 | toggleFavorite |
| 个人资料 | 正常 | getUserProfile / updateProfile |
| 编辑资料 | 正常 | 前端表单 + updateProfile |
| 帮助/关于 | 正常 | 静态页面 |

### 已知未完成的功能

| 功能 | 优先级 | 说明 |
|------|--------|------|
| **教师统计页（teacher_stats）** | P1 | 目前全部使用硬编码 mock 数据，未接入云函数 |
| **职位卡片生成** | P2 | 分享功能当前为文字复制降级方案，需 Canvas 绘制图片 |
| **数据库索引** | P2 | 需在云开发控制台手动创建索引 |
| **ECharts 图表** | P2 | ECharts 已引入但 teacher_stats 暂用 CSS 表格展示 |
| **身份认证流程** | P3 | 当前 userType 由用户自选，无真实身份验证 |
| **敏感字段保护** | P3 | getJobDetail 需根据申请状态过滤 jobLink 和 contactWechat |
---

## 部署操作指南

### 首次部署

1. **创建集合**（云开发控制台 → 数据库）：
   - `users` — 权限：所有用户可读、创建者可读写
   - `jobs` — 权限：所有用户可读
   - `applications` — 权限：仅创建者可读写
   - `notifications` — 权限：仅创建者可读写
   - `favorites` — 权限：仅创建者可读写
   - `userActions` — 权限：仅创建者可读写

2. **部署云函数**（逐个操作，右键 → 上传并部署：云端安装依赖）：
   - 必须逐个部署，**不要批量上传**，否则会卡死
   - 每个云函数都有 `package.json`，依赖 `wx-server-sdk: ~2.6.3`

3. **初始化数据**：
   - 编译小程序 → 自动创建你的用户记录
   - 在 users 集合中把你的 `userType` 改为 `"admin"`
   - 云函数调试面板调用 `initJobs`（入参 `{}`）
   - 调用 `createIndexes`（入参 `{}`）— 如失败则在控制台手动创建索引

4. **开始测试**：
   - 把 `userType` 改为 `"student"` / `"alumni"` / `"teacher"` 体验不同角色

### 重新初始化数据

如果需要重置所有数据，确保你的 userType 为 `"admin"`，然后再次调用 `initJobs`（入参 `{}`）。它会清空 jobs、users、applications、notifications、favorites、userActions 中的种子数据，重新插入。

> **注意**：initJobs 会删除所有种子用户，但不会删除你的真实用户记录（如果它的 _openid 与种子数据不同）。如果需要保留你的记录，请在调用前记下你的 _openid。

---

## 关键文件索引

| 文件 | 用途 |
|------|------|
| `.claude/CLAUDE.md` | Claude Code 入口文件，项目概况和规范索引 |
| `docs/prd.md` | 产品需求文档 |
| `docs/database-schema.md` | 数据库设计文档 |
| `docs/architecture-issues.md` | 架构问题清单 |
| `docs/improvement-plan.md` | 改进实施方案（28 Bug + 7 集合 + 14 云函数） |
| `cloudfunctions/initJobs/index.js` | 种子数据初始化（本次重点修改） |
| `miniprogram/pages/job_detail/job_detail.wxml` | 职位详情页（含内推者有话说模块） |
| `miniprogram/pages/teacher_stats/teacher_stats.js` | 教师统计页（**仍为 mock 数据，待接入云函数**） |

---

## 下一步建议

1. **重新部署云函数** — 右键逐个"上传并部署：云端安装依赖"，特别是 getJobDetail、toggleFavorite、postJob
2. **重新初始化数据** — userType 设 admin → 调用 initJobs({}) → 添加测试数据
3. **teacher_stats 接入云函数** — 新建 getTeacherStats 云函数
4. **职位卡片 Canvas 绘制** — 替换文字复制降级方案
5. **手动创建数据库索引** — 在云开发控制台操作
